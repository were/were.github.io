body { font-family:SimSun }
body { font-family:SimSun }

Title         : A Simple Mail Sever and Client
Author        : 翁健
Logo          : True

[TITLE]

# 前言
SMTP（Simple Mail Transmission Protocol）和POP（Post Office Protocol）
虽然它们在安全性、命令集设定上存在着一些缺陷，但仍然是今天广泛使用的邮件收发协议。
按照Assignment 1的要求，我利用python的`socket`接口实现了一个类`telnet`的命令行工具，
作为SMTP和POP的客户端；同样的，也是利用`socket`接口实现了与它交互的POP与SMTP服务器端。

~ CJK
## Socket的选取
~
因为邮件的收发是借由互联网完成的，所以需要选用`AF_INET`地址家族，
而同时SMTP和POP是两个基于连接的协议，所以应当选取`SOCK_STREAM`套接字。

##消息的结尾
因为邮箱协议要发送邮件，邮件有长有短，所以消息的结束以定长来结束是不可能的，为了解决这个问题，每条消息都以
\\r\\n（即回车和换行）为结尾。

# 客户端
客户端中，我先利用*WireShark*抓包观察`telnet`产生的数据包，每次交互的命令都由命令加参数的形式组成。
然后我利用`socket`连接发送消息的形式，也根据相同的命令制作相同的数据包，从而进行进程间通信。

# SMTP服务器端
根据我对邮件收发整个流程的理解，其实每一层邮件代理服务器，相对于它的下一层，
它自己本身就是一个“客户端”，利用SMTP协议中的`telnet`命令将这层邮件发给下一层。
所以我们并不需要为代理服务器，和最后的接受服务器区分开写不同的服务器端代码。

为了实现SMTP最核心的邮件发送功能，我支持了SMTP的`HELO,MAIL,RCPT,DATA,QUIT,NOOP`这几个核心命令。
和`telnet`中类似，这几个命令都会在服务器完成响应之后，返一个回执给客户端。

具体实现的详情，见下表：

|--------|----------------|------------------------------|
| SMTP命令 |作用              |备注                            |
|--------|----------------|------------------------------|
|        | 进行握手           | 按照标准的SMTP都要传入一个hostname      |
|        | 确认连接           | 但是我发现不管传什么都是和`telnet`开始      |
| HELO   |                | open的那个host进行握手，所以在我的实       |
|        |                | 现里面不传hostname我也进行握手确认连       |
|        |                | 接                            |
|--------|----------------|------------------------------|
|        | 利用from:\<xxx\> | 如果在完成了发件人的协商之后再次企图           |
| MAIL   | 参数进行发件人        | 调用这个参数则会引发错误                 |
|        | 的协商            |                              |
|--------|----------------|------------------------------|
|        | 利用to:\<xxx\>   | 如果没有完成发件人的协商就企图进行收           |
| RCPT   | 参数进行收件人        | 件人的协商，则会引发错误；收件人可以           |
|        | 的协商            | 有多个                          |
|--------|----------------|------------------------------|
|        | 进行邮件正文         | 发件人和收件人两者缺一不可，否则会引           |
| DATA   | 内容的协商          | 发错误，以一个单个一行的（"\\r\\n.\\r\\n"）|
|        |                |为结束                           |
|--------|----------------|------------------------------|
| QUIT   | 结束会话           | 同时退出客户端                      |
|--------|----------------|------------------------------|
| NOOP   | 啥也不做           | 通常用于确认连接的完整性                 |
|--------|----------------|------------------------------|

附表，依照SMTP标准，返回code以表示通信结果：

|---|-------|
|编号 |原因     |
|---|-------|
|220| 连接成功  |
|250| 操作成功  |
|354|开始协商   |
|   | 邮件正文  |
|503| 其他错误  |
|501|命令语法错误 |
|---|-------|

为了证明我的邮箱有收件功能，在协商完邮件正文之后，我就会在服务器本地开辟一个文件夹，
为`REPT`的目标存下邮件，以便用于后面测试POP的功能。

# POP服务器端
